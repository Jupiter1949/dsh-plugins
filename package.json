{
  "name": "dsh-plugins",
  "private": true,
  "description": "DSH plugin monorepo: cot-smart and future DeepSeek Harness plugins",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check"
  }
}
